name: blog_frontend
description: A new Flutter project.

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  provider: ^6.1.5
  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true
